import React from "react";
import "./Hero.css";

const Hero = () => {
  return (
    <div className="container">
      <btn className="btn btn-newUser">Primeira vez aqui</btn>
      <btn className="btn btn-primary">Ja conheço</btn>
    </div>
  );
};

export default Hero;
